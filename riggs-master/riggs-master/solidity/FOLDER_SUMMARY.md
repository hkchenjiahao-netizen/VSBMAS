# 文件夹摘要：`solidity`

## 用途
**Solidity 合约与 Rust 封装**：`contracts/`（如 `TC.sol`、`RSA2048.sol`、`PedersenBaby.sol`、`PoEVerifier.sol`）、`src/lib.rs`、`benches/`。

## AI 检索
链上验证、合约 ABI、gas 基准 → **`contracts/`** + **`src/`**。
