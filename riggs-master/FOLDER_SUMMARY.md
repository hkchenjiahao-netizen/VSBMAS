# 文件夹摘要：`riggs-master`

## 用途
外层目录：**内含一层 `riggs-master/riggs-master`** 才是真正的 Cargo workspace 根（见内层 `Cargo.toml`）。

## AI 检索
**所有 Riggs crate 在内层 `riggs-master/`**。外层仅便于解压归档；编译请在 **`riggs-master/riggs-master`** 执行。
