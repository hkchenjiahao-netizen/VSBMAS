# 文件夹摘要：`vsbmas_round`

## 用途
**多轮拍卖**：`RoundManager`、`RoundConfig`、`MultiRoundStatus`（名称以 `src/lib.rs` 为准）。

## 关键文件
- `src/lib.rs` — 轮次推进与总时长兜底等逻辑入口。

## AI 检索
扩展「第 13 步 RoundManager」或调试多轮状态 → **此 crate**。
