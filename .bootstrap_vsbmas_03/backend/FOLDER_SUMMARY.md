# 文件夹摘要：`backend`

## 用途
脚手架中的 **Axum 后端 crate**：路由、`main.rs` 入口；依赖 `vsbmas_core`、`vsbmas_round`。

## 关键路径
- `src/main.rs` — HTTP/WebSocket、REST handler 骨架。
- `Cargo.toml` — crate 名称与依赖。

## AI 检索
与 **`_wsl_sync`** 中后端对照阅读；后者常为完整演示版本。
